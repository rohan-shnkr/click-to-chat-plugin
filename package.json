{
    "name": "gemini-text-explainer",
    "version": "1.0.0",
    "description": "Chrome extension for explaining text using Gemini AI",
    "private": true,
    "scripts": {
      "build": "rollup -c",
      "watch": "rollup -c -w"
    },
    "dependencies": {
      "@google/generative-ai": "^0.1.3"
    },
    "devDependencies": {
      "rollup": "^4.9.0"
    }
  }